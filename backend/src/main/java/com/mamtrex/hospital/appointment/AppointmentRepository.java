package com.mamtrex.hospital.appointment; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface AppointmentRepository extends JpaRepository<Appointment,UUID>{ /* Branch-scoped workflow reads (docs/plan3.md Task 4): a cross-branch id resolves to empty, indistinguishable from nonexistent. */ Optional<Appointment> findByIdAndBranchId(UUID id,UUID branchId); List<Appointment> findByBranchIdOrderByIdAsc(UUID branchId); }
