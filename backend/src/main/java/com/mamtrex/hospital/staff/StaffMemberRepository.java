package com.mamtrex.hospital.staff; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface StaffMemberRepository extends JpaRepository<StaffMember,UUID>{ /* Branch-scoped workflow reads (docs/plan3.md Task 4): a cross-branch id resolves to empty, indistinguishable from nonexistent. */ Optional<StaffMember> findByIdAndBranchId(UUID id,UUID branchId); List<StaffMember> findByBranchIdOrderByIdAsc(UUID branchId); }
