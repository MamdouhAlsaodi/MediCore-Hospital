# Phase 3 browser evidence

These screenshots are generated by the Task 13 Playwright journey only after both the desktop and mobile projects pass every behavior, accessibility, responsive-layout, branch-isolation, expected-conflict, console, and page-error assertion.

## Source state

- Branch: `test/responsive-multi-branch-browser-evidence`
- Task baseline: `942bb6bf90703205185da5991b953aa0f3cb167e`
- Candidate: the Task 13 branch state published by its PR; the images are generated before commit so the evidence file cannot truthfully embed its own final commit hash.
- Runtime: disposable loopback-only H2 backend at `127.0.0.1:5591` and Vite frontend at `127.0.0.1:5592`.
- Data boundary: synthetic `Demo*` / `DEMO-*` fixtures only. No real patient, staff, clinical, financial, credential, or production data is present.

## Evidence set

### `branch-command-center-desktop.png`

- Tested viewport: 1280 × 720.
- Image dimensions: 1280 × 720.
- Journey step: authenticated ADMIN dashboard on `Demo Main Branch`.
- Proves: current acting branch and role, three-branch network comparison, readable desktop layout, and synthetic-only command-center values.

### `branch-command-center-mobile.png`

- Tested viewport: 375 × 812.
- Image dimensions: 375 × 812.
- Journey step: authenticated ADMIN dashboard on `Demo Main Branch`.
- Proves: visible branch context, operable single-line scrolling navigation, readable command-center cards, and no page-level horizontal overflow at the narrow viewport.

### `bed-transfer-mobile.png`

- Tested viewport width: 375; captured as a full-page image after the verified mobile journey.
- Image dimensions: 375 × 1009.
- Journey step: `Demo Patient Echo` transferred atomically to `Bed 02 — Demo Ward N · 201` on `Demo North Branch`.
- Proves: branch context and transferred result in one artifact, readable labelled admission cards rather than compressed table columns, and synthetic-only bed/admission state.

## Inspection record

The complete set was directly inspected after the passing browser run. No clipped core action, unreadable admission value, stale-branch content, real personal data, credential, private endpoint, or unsupported production claim was observed. Automated checks supplement this visual review; they do not replace it.

Reproduce with the isolated procedure in [`../../runbook.md`](../../runbook.md). A failed or partial run deletes stale published images and does not copy a replacement set into this directory.
